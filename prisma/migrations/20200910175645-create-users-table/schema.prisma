generator client {
  provider = "prisma-client-js"
}

///
datasource db {
  url = "***"
  provider = "postgresql"
}

model User {
  id        Int      @default(autoincrement()) @id
  username  String
  createdAt DateTime @default(now())
}
